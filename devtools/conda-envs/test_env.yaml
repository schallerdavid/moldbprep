name: test
channels:
dependencies:
    # Base depends
  - python >= 3.5
  - pip
  - molvs >= 0.1.1

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

